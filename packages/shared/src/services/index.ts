/**
 * Shared services across the monorepo
 * Add common service functions here (auth, content, etc.)
 */

// Placeholder for future shared services
export const sharedServices = {
  // Add shared service implementations here
};
